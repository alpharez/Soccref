<div class="row-fluid">
  <div class="span6">
    <h1><%= link_to @user.email, @user %></h1>
    <% if @user.name? %>
      <h3><%= @user.name %></h3>
    <% end %>
  </div> <!-- span -->
  <div class="span3">
    <ul class="nav nav-pills nav-stacked">
      <li><%= link_to "Edit Profile", edit_user_path(@user) %></li>
      <li><!-- %= link_to "Send Invite", new_user_invitation_path %-- >disabled invitations</li>
      <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
    </ul>
  </div> <!-- span -->
</div> <!-- row -->
<div class="row-fluid">
  <div class="span6">
    <% if @referees.any? %>
      <h3>Favorite Refs</h3>
    <% end %>
  </div> <!-- span -->
</div> <!-- row -->
